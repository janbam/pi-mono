# Changelog

## [Unreleased]

### Added

- Initial release: `pless` CLI that pages Markdown files through pi-tui's markdown renderer in an alternate-screen viewport with less-style scrolling, built-in overlay search, and multi-file navigation via `:n`/`:p`. Colors follow the coding agent's configured theme (including custom themes and terminal light/dark detection).
